O QUE PRECISO AGORA:
1. Criar controller web CertificatesController (herdando de WebController) com ações: index, show, destroy, resend_email, download
2. Criar views ERB para index e show (Bootstrap 5, em português)
3. Instalar e configurar gems: 'wicked_pdf' + 'wkhtmltopdf-binary' (ou 'prawn' se preferir) para gerar PDF
4. Instalar e configurar gem 'rqrcode' para gerar QR Code como imagem PNG/SVG
5. Na view show do certificado, mostrar:
   - Dados do evento e participante
   - UUID
   - QR Code renderizado como imagem
   - Botão "Baixar PDF"
   - Botão "Reenviar Email"
   - Histórico de logs de envio
6. O PDF deve usar o template em certificate_templates/default/template.html.erb (já existe) e preencher variáveis dinâmicas
7. O QR Code no PDF deve conter o JSON com todos os dados do certificado (já está em certificate.qr_code_data)
8. Adicionar link "Certificados" na navbar do layout application.html.erb

ESTRUTURA EXISTENTE:
- app/models/certificate.rb (tem uuid, qr_code_data, event, participant, email_logs)
- app/views/layouts/application.html.erb (navbar com Eventos, Participantes)
- certificate_templates/default/template.html.erb (template ERB do certificado)
- config/routes.rb já tem resources :certificates com member { post :resend_email, get :download }

IMPORTANTE:
- O PDF deve ser gerado na ação download e enviado como arquivo para download
- O QR Code deve ser renderizável tanto na web quanto no PDF
- Usar helpers de rotas corretamente
- Mensagens flash para feedback ao usuário

Me forneça o código completo: Gemfile (linhas das gems), CertificatesController, views em app/views/certificates/, e configuração inicial dos geradores de PDF e QR Code